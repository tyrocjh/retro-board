import React, { Component } from 'react';
import { IconButton } from 'react-toolbox';

export default class DrawerButton extends Component {
	render() {
		return(
			<IconButton icon='dehaze' inverse />
		);
	}
}
